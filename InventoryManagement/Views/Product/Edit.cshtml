@model ProductModel
@{
    ViewData["Title"] = "Edit Product";
    var categories = ViewBag.Categories as List<CategoryModel>;
}
<h2>Edit Product</h2>
<form method="post">
    <input type="hidden" asp-for="ProductId" />
    <div class="form-group">
        <label asp-for="ProductName"></label>
        <input asp-for="ProductName" />
        <span asp-validation-for="ProductName"></span>
    </div>
    <div class="form-group">
        <label asp-for="CategoryId"></label>
        <select asp-for="CategoryId" asp-items="@(new SelectList(categories, "CategoryId", "CategoryName"))"><option value="">Select Category</option></select>
        <span asp-validation-for="CategoryId"></span>
    </div>
    <div class="form-group">
        <label asp-for="Price"></label>
        <input asp-for="Price" />
        <span asp-validation-for="Price"></span>
    </div>
    <div class="form-group">
        <label asp-for="Quantity"></label>
        <input asp-for="Quantity" />
        <span asp-validation-for="Quantity"></span>
    </div>
    <button type="submit" class="btn btn-success">Update</button>
    <a href="@Url.Action("Index")" class="btn btn-primary">Cancel</a>
</form>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}